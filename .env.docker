# Docker Environment Variables
# Generate a secure SECRET_KEY with: openssl rand -hex 32

SECRET_KEY=replace-this-with-secure-secret-key-from-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database credentials (must match docker-compose.yml)
POSTGRES_USER=sensor_user
POSTGRES_PASSWORD=sensor_password
POSTGRES_DB=sensor_db
